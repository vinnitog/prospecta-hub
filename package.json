{
  "name": "prospecta-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CRM local de prospecção com integração isolada à Meta Cloud API",
  "engines": {
    "node": ">=24.12.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "test": "node --test unit/*.test.js",
    "check": "node --check src/server.js && node --check src/store.js && node --check src/whatsapp.js && node --check public/app.js"
  }
}
